// break fires AFTER the printf on i==3, so the sequence is 0,1,2,3
// then the loop exits. Expected: "0, 1, 2, 3, " followed by newline.
for(int i=0; i<5; i+=1) {
    printf("%d, ", i);
    if(i==3) {
        break;
    }
}
printf("\n");
exit(0);
