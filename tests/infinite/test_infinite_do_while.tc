// Infinite do-while — body runs at least once regardless of condition,
// then the condition is always true so it never exits.
// Expected: exit code 1, stderr contains
// "do-while loop exceeded 10000000 iterations".
int i = 0;
do {
    i += 1;
} while (1);
exit(i);