// Infinite for — empty condition means "always true" (C semantics).
// Expected: exit code 1, stderr contains
// "for loop exceeded 10000000 iterations".
int total = 0;
for (int i = 0; ; i += 1) {
    total += 1;
}
exit(total);